---
description: Snapshot the current session state into the mind-map right now
---

Update `.claude/mindmap.md` immediately with the true current state of this session (create the file with the three sections if it is missing):

- **🎯 Active Focus** — rewrite it: the exact task in progress, the files being touched, and the current approach. 3–6 bullets.
- **🪦 Graveyard** — append anything tried in this session that failed or was abandoned, one line each: `approach — why it failed`. Keep every existing entry.
- **📌 Pending Work** — refresh it: unresolved bugs, loose ends, and the immediate next steps in priority order. Remove items that are done.
- Update the `_Last updated:_` line with the current date/time and a few words of context.

Keep the whole file under 120 lines — prune stale Active Focus details, merge duplicate Graveyard entries, keep one line per item.

$ARGUMENTS

If arguments were given above, treat them as extra notes from the user to fold into the right sections. When done, confirm with one line summarizing what changed in the map.
